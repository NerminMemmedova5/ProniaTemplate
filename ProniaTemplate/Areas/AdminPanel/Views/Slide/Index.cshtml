@model List<Slide>

@{
    int count = 1;
}

<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">

            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Author</h4>

                        <a asp-action="create"> <button type="button" class="btn btn-secondary">Create</button></a>

                        <div class="table-responsive pt-3">
                            <table class="table table-dark">
                                <thead>
                                    <tr>
                                        <th>
                                            #
                                        </th>
                                        <th>
                                            Image
                                        </th>
                                        <th>
                                            Title
                                        </th>
                                        <th>
                                            SubTitle
                                        </th>
                                        <th>
                                            Description
                                        </th>
                                        <th>
                                            Setting
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>
                                                @(count++)
                                            </td>
                                            <td>
                                                <img src="~/upload/slide/@item.Image" alt="">
                                            </td>
                                            <td>
                                                @item.Title
                                            </td>
                                            <td>
                                                @item.SubTitle
                                            </td>
                                            <td>
                                                @item.Description
                                            </td>
                                            <td>

                                                <a asp-controller="slide" asp-action="update" asp-route-id="@item.Id" class="btn btn-warning"><i class="mdi mdi-pencil"></i> </a>
                                                <a asp-controller="slide" asp-action="delete" asp-route-id="@item.Id" class="btn btn-danger btn-item-delete"><i class="mdi mdi-delete"></i></a>
                                                <a class="btn btn-primary"><i class="mdi mdi-open-in-new"></i></a>


                                            </td>
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
    <!-- content-wrapper ends -->

</div>
